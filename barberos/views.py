from django.http import HttpResponse


def placeholder_login(request):
    return HttpResponse('Acceso Barberos — se implementa en el Paso 3.')


def placeholder_dashboard(request):
    return HttpResponse('Dashboard — se implementa en el Paso 6.')
